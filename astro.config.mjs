import { defineConfig } from 'astro/config';

// Troque pela URL final do seu site no Cloudflare Pages (ou seu domínio próprio)
export default defineConfig({
  site: 'https://blogg.chais1eee2.workers.dev',
});

