---
import BaseLayout from './BaseLayout.astro';
import Giscus from '../components/Giscus.astro';
import { slugifyTag } from '../utils/slugifyTag';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  updatedDate?: Date;
  tags: string[];
}

const { title, description, pubDate, updatedDate, tags } = Astro.props;

const dataFormatada = pubDate.toLocaleDateString('pt-BR', {
  day: '2-digit',
  month: 'short',
  year: 'numeric',
});
---
<BaseLayout title={`${title} — artigos.lab`} description={description}>
  <div class="wrap">
    <header class="article-header">
      <p class="post-meta">
        {dataFormatada}
        {tags.map((tag) => (
          <>
            {' · '}
            <a class="tag" href={`/tags/${slugifyTag(tag)}/`}>#{tag}</a>
          </>
        ))}
      </p>
      <h1>{title}</h1>
      <p class="desc">{description}</p>
    </header>

    <article class="prose">
      <slot />
    </article>

    <Giscus />
  </div>
</BaseLayout>